# Formalization status

The current development covers adjacent-gap signatures, midpoint injectivity, the three-signature fractional-cover lemma, the clipped ramp, residue-class evaluations, and the exact cutoff comparison. CI is being used to resolve all remaining kernel errors before adding the squarefree divisor bridge and the final theorem about `tauPerp` and `g`.
